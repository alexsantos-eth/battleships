import GameGrid from "./components/GameGrid";
import EnvironmentBox from "./env";

const App = () => {
  return (
    <EnvironmentBox>
      <GameGrid />
    </EnvironmentBox>
  );
};

export default App;
